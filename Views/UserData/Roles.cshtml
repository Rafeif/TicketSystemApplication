@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

@{
    ViewData["Title"] = "Roles List";
}

<h1>Roles</h1>

@*<p>
    <a asp-action="CreateRole">Add New Role + </a>
</p>*@
<table class="table ">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>


        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>


                <td>
                    <table>
                   @*     <tr>
                            <a asp-action="EditRole" asp-route-id="@item.Id" class="btn btn-outline-primary me-2">Edit</a>
                        </tr> 
                        <tr>
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-outline-success me-2">Details</a>
                        </tr> *@
                      @*  <tr>
                            <form asp-action="DeleteRole" asp-route-id="@item.Id" method="post">
                                <input type="submit" class="btn btn-outline-danger me-2" value="Delete" onclick="return confirm('Are you sure you want to delete this item?') " />
                            </form>
                        </tr>*@
                    </table>
           
               
            
                </td>
            </tr>
        }
    </tbody>
</table>
